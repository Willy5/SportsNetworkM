{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FriendlyMatch list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Summary</th>
                <th>Date</th>
                <th>Begintime</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('friendlymatch_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.summary }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.beginTime }}</td>
                <td>{{ entity.status }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('friendlymatch_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('friendlymatch_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('friendlymatch_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
