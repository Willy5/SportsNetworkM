{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Sport list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Nbplayersmin</th>
                <th>Nbplayersmax</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('sport_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.nbPlayersMin }}</td>
                <td>{{ entity.nbPlayersMax }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('sport_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('sport_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('sport_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
