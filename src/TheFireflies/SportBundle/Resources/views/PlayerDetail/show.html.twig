{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PlayerDetail</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Playernumber</th>
                <td>{{ entity.playerNumber }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ entity.position }}</td>
            </tr>
            <tr>
                <th>Nameifguest</th>
                <td>{{ entity.nameIfGuest }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('playerdetail') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('playerdetail_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
